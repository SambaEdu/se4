# Rotation des logs de se3
/var/log/se3/*.log {
        daily
        rotate 7
        compress
        notifempty
        missingok
}

# Rotation des logs clients
/var/log/samba/*.log {
	daily
	rotate 7
	compress
	notifempty
	missingok
}
